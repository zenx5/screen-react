//Reducers
const initialState = {
    
}

export default function routerReducer( state = initialState, action){
    const { type } = action
    switch( type ) {
        
        default:
            return state
    }
}